import { AssessmentDirective } from './assessment.directive';

describe('AssessmentDirective', () => {
  it('should create an instance', () => {
    const directive = new AssessmentDirective();
    expect(directive).toBeTruthy();
  });
});
